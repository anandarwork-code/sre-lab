global:
  scrape_interval: 15s

scrape_configs:
  - job_name: prometheus_monitor
    static_configs:
      - targets: ['localhost:9090']

{% for host in groups['all'] %}
  - job_name: {{ host }}_monitor
    static_configs:
      - targets: ['{{ hostvars[host]['ansible_host'] }}:9100']
{% endfor %}
